{
  "name": "clin",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "prepare": "husky",
    "check": "npm run typecheck --prefix web && npm run lint --prefix web",
    "dev": "npm --prefix web run dev",
    "dev:stop": "npm --prefix web run dev:stop",
    "dev:clean": "npm --prefix web run dev:clean",
    "build": "npm --prefix web run build",
    "build:desktop:web": "npm --prefix web run build:desktop",
    "build:desktop:prepare": "node scripts/prepare-electron-app.mjs",
    "build:desktop": "npm run build:desktop:web && npm run build:desktop:prepare",
    "dist:desktop": "npm run build:desktop && electron-builder --publish never",
    "dist:desktop:win": "npm run build:desktop && electron-builder --win portable zip --publish never",
    "dist:desktop:mac": "npm run build:desktop && electron-builder --mac zip --publish never",
    "dist:extension": "node scripts/zip-extension.mjs",
    "desktop:start": "electron .",
    "start": "npm --prefix web run start",
    "lint": "npm --prefix web run lint",
    "typecheck": "npm --prefix web run typecheck",
    "db:repair": "npm --prefix web run db:repair",
    "db:generate": "npm --prefix web run db:generate",
    "db:push": "npm --prefix web run db:push",
    "db:studio": "npm --prefix web run db:studio"
  },
  "devDependencies": {
    "electron": "^37.1.0",
    "electron-builder": "^26.0.12",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  },
  "build": {
    "afterPack": "./scripts/electron-after-pack.cjs",
    "publish": null,
    "appId": "com.clin.desktop",
    "productName": "Clin",
    "directories": {
      "output": "dist/desktop"
    },
    "files": [
      "electron/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "desktop-app",
        "to": "desktop-app",
        "filter": ["**/*"]
      }
    ],
    "asar": true,
    "mac": {
      "target": [
        "zip"
      ],
      "category": "public.app-category.productivity",
      "identity": "-",
      "gatekeeperAssess": false
    },
    "win": {
      "target": [
        "portable",
        "zip"
      ]
    },
    "portable": {
      "artifactName": "Clin-${version}-windows-portable.${ext}"
    }
  }
}
